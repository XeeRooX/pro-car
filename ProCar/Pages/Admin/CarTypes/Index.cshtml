@page
@model ProCar.Pages.Admin.CarTypes.IndexModel
@{
    Layout = "_Layout2";
}
<nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-page="/Admin/Index">Панель администратора</a></li>
        <li class="breadcrumb-item active" aria-current="page">Типы автомобилей</li>
    </ol>
</nav>

<h2>Список типов автомобилей</h2>
<p class="mt-3">
    Здесь Вы можете просмотреть уже существующую информацию о типе машин. Также Вы можете отредактировать уже существующие данные или добавить новые
</p>
<h3>Добавить новый тип</h3>
<form method="post" asp-page="/Admin/CarTypes/Index">
    <div class="input-group my-3">
        <input class="form-control" asp-for="Input.Name" required maxlength="50"/>
        <button class="btn btn-primary" type="submit">Добавить</button>
    </div>
    <span asp-validation-for="Input.Name"></span>
    <span>@Model.ErrorMsg</span>
</form>

<div class="table-responsive small">
    <table class="table table-striped table-sm">
        <thead>
            <tr>
                <th scope="col">Название</th>
                <th scope="col">Действия</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var item in Model.CarTypes)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>
                            <a asp-page="/Admin/CarTypes/Edit" asp-route-id="@item.Id">Редактировать</a> | 
                            <a asp-page="/Admin/CarTypes/Delete" asp-route-id="@item.Id">Удалить</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>