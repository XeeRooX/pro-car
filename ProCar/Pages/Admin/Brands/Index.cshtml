@page
@model ProCar.Pages.Admin.Brands.IndexModel
@{
  Layout = "_Layout2";
}
<nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-page="/Admin/Index">Панель администратора</a></li>
        <li class="breadcrumb-item active" aria-current="page">Марки</li>
    </ol>
</nav>

<h2>Список марок автомобилей</h2>

<p class="mt-3">
    Здесь Вы можете просмотреть уже существующую информацию о марках. Также Вы можете отредактировать уже существующие данные или добавить новые
</p>

<a class="btn btn-secondary" href="/admin/brands/create">Добавить новую марку</a>
<div class="table-responsive small mt-3">
    <table class="table table-striped table-sm">
        <thead>
            <tr>
                <th scope="col">Фото</th>
                <th scope="col">Название</th>
                <th scope="col">Действия</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var item in Model.Brands)
                {
                                <tr>
                                    <td><img src="/Data/imgs/brands/@(item.Id).png" style="width: 4rem" /></td>
                                    <td>@item.Name</td>
                                    <td>
                                        <a asp-page="/Admin/Brands/Edit" asp-route-id="@item.Id">Редактировать</a> | 
                                        <a asp-page="/Admin/Brands/Delete" asp-route-id="@item.Id">Удалить</a>
                                    </td>
                                </tr>
                }
            }
        </tbody>
    </table>
</div>